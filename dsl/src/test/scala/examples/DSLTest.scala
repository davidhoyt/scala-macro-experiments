package examples

import org.scalatest.junit.JUnitRunner
import org.scalatest.{BeforeAndAfterAll, SeveredStackTraces, FunSuite}
import org.junit.runner.RunWith
import org.scalatest.matchers.ShouldMatchers

@RunWith(classOf[JUnitRunner])
class DSLTest extends FunSuite
with ShouldMatchers
with SeveredStackTraces {
  test("foo") {
//    val b = "b"
//
//    val foos = CREATE TABLE t"1abc how are you" (
//      c"empno" NUMBER(5) PRIMARY KEY,
//      c"empno2" NUMBER(5) PRIMARY KEY,
//      PRIMARY KEY(pk"empno", pk"empno2")
//    )
  }
}


